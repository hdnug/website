@model Hdnug.Web.Areas.Admin.Models.ViewModels.SpeakerViewModel

@{
    ViewBag.Title = "Create";
}

<h3>Create Speaker</h3>
<hr/>
@using (Html.BeginForm("Create", "Speakers", FormMethod.Post, new { id = "uploadForm", enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        @Html.ValidationSummary(true)

        @Html.Partial("_Speaker", Model)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="button" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
